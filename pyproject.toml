[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "connectivity"
version = "0.1.0"
description = "EEG connectivity analysis toolbox"
authors = [{ name = "Roland D. Widmer" }]
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["connectivity*"]
